name: Test CICD

on:
  push:
    branches: [main]

jobs:
  test_cicd:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: ls
        run: ls -la

